---
description: Reuse Gradle/NFRT caches; wait for the user to close the game before datagen
alwaysApply: true
---

# Datagen cache and game lock

Run datagen with `.\gradlew runData` from the repo root (never `clean` / `--rerun-tasks` / `--refresh-dependencies` / `--offline`). See AGENTS.md.

A running `runClient` / `runServer` locks `build/`. Datagen cannot run at the same time.

**Do not** kill Java/Gradle, `gradlew --stop`, or start/restart the client. Tell the user to close Minecraft, then wait:

```
.\scripts\wait-until-game-closed.ps1; .\gradlew.bat runData
```

Use a long `block_until_ms` (600000) and `AwaitShell` until the wait script exits 0, then until `runData` succeeds. You are not done until datagen finishes. If `runData` fails with file-in-use / lock / unable to delete, wait and retry.

Do not:

- `gradlew clean` / `clean runData`
- `--rerun-tasks`, `--refresh-dependencies`, `--offline`
- delete `src/generated/` or `src/generated/resources/.cache`
- hand-write `src/generated/**` JSON to avoid running datagen

`build.gradle` pins NeoForm Runtime to `%USERPROFILE%\.gradle\caches\neoformruntime` so a Cursor sandbox (temp `GRADLE_USER_HOME`) still reuses Minecraft assets and decompiles.

Abort if logs show `cursor-sandbox-cache` **and** `downloadAssets` counting thousands of files — that is a cache miss. Fix the pin or rerun so NFRT uses the user Gradle cache; do not wait out a full asset download.
